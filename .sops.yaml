# SOPS configuration for secret encryption with age
# Generate a key: age-keygen -o age.key
# Add the public key below, keep age.key out of git (.gitignore it)
creation_rules:
  - path_regex: .*/secrets/.*\.yaml$
    encrypted_regex: ^(data|stringData)$
    age: >-
      age1x23xy95kpc6h3ql9x4q35r8r2g5yd0mjejggz55a2mnc5muwys2qreflxt
